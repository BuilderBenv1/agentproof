# Agent402 — Trust Oracle Configuration
# Copy to .env and fill in values

# ─── Supabase (Agent402's own database) ──────────────────────────────
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_KEY=your-service-role-key

# ─── x402 Payment Protocol ──────────────────────────────────────────
# Wallet address that receives USDC payments (Base network)
X402_PAY_TO=0xYourWalletAddress

# Network: eip155:84532 = Base Sepolia (testnet), eip155:8453 = Base (mainnet)
X402_NETWORK=eip155:84532

# Coinbase facilitator URL
# Testnet: https://x402.org/facilitator
# Production: https://api.cdp.coinbase.com/platform/v2/x402
X402_FACILITATOR_URL=https://x402.org/facilitator

# Pricing (USD)
X402_PRICE_EVAL=$0.01
X402_PRICE_SEARCH=$0.01
X402_PRICE_STATS=$0.005

# ─── Oracle Identity ─────────────────────────────────────────────────
ORACLE_NAME=Agent402 Trust Oracle
ORACLE_VERSION=1.0.0

# ─── Server ──────────────────────────────────────────────────────────
HOST=0.0.0.0
PORT=8402
BASE_URL=https://agent402.sh
CORS_ORIGINS=http://localhost:3000,https://agent402.sh

# ─── Wallet (for signing, optional) ─────────────────────────────────
PRIVATE_KEY=
