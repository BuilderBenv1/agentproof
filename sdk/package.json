{
  "name": "@agentproof/sdk",
  "version": "0.1.0",
  "description": "TypeScript SDK for AgentProof — transparent reputation for AI agents on Avalanche",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc",
    "test": "ts-mocha tests/**/*.test.ts --timeout 30000",
    "lint": "tsc --noEmit"
  },
  "keywords": ["agentproof", "avalanche", "ai-agents", "reputation", "erc-8004"],
  "license": "MIT",
  "dependencies": {
    "ethers": "^6.13.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "@types/node": "^20.0.0",
    "mocha": "^10.7.0",
    "@types/mocha": "^10.0.0",
    "ts-mocha": "^10.0.0",
    "chai": "^4.4.0",
    "@types/chai": "^4.3.0"
  }
}
